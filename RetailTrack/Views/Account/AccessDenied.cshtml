@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container text-center mt-5">
    <div class="card shadow-lg p-4">
        <div class="card-body">
            <i class="fas fa-exclamation-triangle fa-3x text-warning"></i>
            <h2 class="text-danger mt-3">Acceso Denegado</h2>
            <p class="text-muted">Lo sentimos, pero no tienes permisos para acceder a esta sección.</p>
            <a href="@Url.Action("Index", "Home")" class="btn btn-primary">Volver al Inicio</a>
            <a href="@Url.Action("Login", "Account")" class="btn btn-secondary">Iniciar Sesión con otra cuenta</a>
        </div>
    </div>
</div>
